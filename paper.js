class Paper{
    constructor(x, y, width, height, angle) {
        var options = {
            isStatic:false,
            restitution:0.3,
            friction:1.0,
            density:1.2
        }
    }
      display(){
        push();
        pop();
      }
}